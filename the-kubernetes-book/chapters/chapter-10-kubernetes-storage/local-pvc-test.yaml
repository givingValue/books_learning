apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-test
  annotations:
    volume.kubernetes.io/selected-node: "gt-001"
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path-immediate
  resources:
    requests:
      storage: 10Gi