apiVersion: v1
kind: Service
metadata:
  name: git-sync-svc-nodeport
  namespace: kubernetes-book
spec:
  type: NodePort
  selector:
    app: sidecar
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30000