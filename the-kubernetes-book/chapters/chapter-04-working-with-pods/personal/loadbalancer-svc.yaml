apiVersion: v1
kind: Service
metadata:
  name: git-sync-svc-loadbalance
  namespace: kubernetes-book
spec:
  type: LoadBalancer
  selector:
    app: sidecar
  ports:
    - name: http
      port: 80
      targetPort: 80